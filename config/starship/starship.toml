add_newline = true
format = '[#](dimmed) $directory$git_branch$character'

[directory]
format = '[$path ](yellow)'

[git_branch]
format = '[$branch(:$remote_branch) ](purple)'

[character]
format = '$symbol'
success_symbol = ''
error_symbol = '[×](bold red) '