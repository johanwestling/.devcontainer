# Devcontainer

This is a template for setting up or enabling devcontainer support for any project to ease the initial setup.

```bash
git clone git@github.com:johanwestling/.devcontainer.git && .devcontainer/settings.sh
```
