# Devcontainer starter

This repository is a starter for creating a `.devcontainer` for any project and provide a decent starting point by doing the basic host mounting to work with ssh, git and docker from the host.

